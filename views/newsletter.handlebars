<div class="px-2 pt-8 pb-24">
  <div class="container mx-auto">
    <h2 class="text-2xl">Subscribe to the newsletter</h2>
    <p class="mt-2 text-lg text-gray-600 leading-normal">
      Get updates about upcoming events, new content and more!
    </p>
    <form action="/newsletter/subscribe" method="post" class="w-1/3">
      <input name="email" type="text" placeholder="Enter your email..." class="mt-8 appearance-none block w-full bg-white placeholder-gray-600 border border-gray-400 rounded py-3 px-4 leading-none focus:outline-none focus:bg-white focus:border-red-600" />
      {{#if errorMessage}}
      <p class="mt-2 text-sm text-red-700">{{errorMessage}}</p>
      {{/if}}
      <button type="submit" class="inline-flex items-center mt-4 px-4 py-3 leading-none no-underline rounded bg-red-600 hover:bg-red-700 text-white">
        Subscribe
      </button>
    </form>
    {{#if newsletterErrorMessage}}
    <p class="mt-2 text-white font-bold">{{newsletterErrorMessage}}</p>
    {{/if}}
  </div>
</div>